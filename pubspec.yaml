name: project_tutorial
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=2.19.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  image_picker:
  shared_preferences: ^2.0.17
  path_provider: ^2.0.1
  google_nav_bar: ^5.0.6
  firebase_auth: ^4.2.8
  provider: ^6.0.5
  cloud_firestore: ^4.4.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

assets:
  - images/
  - images/profileImg.png
